package interface_adapter.logged_in;

/**
 * The Controller for the logged-in user. No data needs to be passed since the logged in view is static.
 * So this is empty. We keep it for CA.
 */

public class HomeController {

}
